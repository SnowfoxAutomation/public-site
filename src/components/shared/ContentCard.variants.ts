export const contentCardVariants = {
  root: [
    "rounded-[var(--radius-large)]",
    "border",
    "border-border",
    "bg-surface",
    "p-8",
    "shadow-[var(--shadow-card)]",
  ].join(" "),
} as const;